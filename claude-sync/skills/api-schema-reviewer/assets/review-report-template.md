# API Schema Review

**Schema:** `<file or API name>`
**Style:** `<REST / OpenAPI / GraphQL / gRPC>`
**Scope:** `<full spec | PR/diff: describe what's changing>`
**Reviewed against:** industry best practices (Google API Design Guide / AIPs, Microsoft REST API Guidelines, Zalando RESTful Guidelines, GraphQL best practices, protobuf style guide)

---

## Summary

- **Verdict:** `<ship-ready | needs revisions before merge | needs significant rework>`
- **Backwards-compatible** (if reviewing a diff): `<yes | no — details below>`

| Severity | Count |
| -------- | ----- |
| blocker  | 0     |
| major    | 0     |
| minor    | 0     |
| nit      | 0     |

**Top themes:**

1. `<theme>`
2. `<theme>`
3. `<theme>`

---

## Findings

### Blockers

#### B1. `<short title>`

- **Location:** `<exact schema path>`
- **Category:** `<naming | http-semantics | error-model | pagination | versioning | security | data-model | documentation | backwards-compat | consistency>`
- **Finding:** `<one sentence: what's wrong>`
- **Why it matters:** `<one sentence: concrete consequence>`
- **Suggested fix:**
  ```
  <copy-pasteable fix when possible>
  ```

### Majors

#### M1. `<short title>`

- **Location:** `<...>`
- **Category:** `<...>`
- **Finding:** `<...>`
- **Why it matters:** `<...>`
- **Suggested fix:** `<...>`

### Minors

#### m1. `<short title>`

- **Location:** `<...>`
- **Category:** `<...>`
- **Finding:** `<...>`
- **Why it matters:** `<...>`
- **Suggested fix:** `<...>`

### Nits

- `<location>` — `<one-line finding and suggestion>`
- `<location>` — `<one-line finding and suggestion>`

---

## What's working well

- `<brief positive callout, if applicable>`
- `<brief positive callout, if applicable>`

---

## Notes for the author

`<any follow-up questions, unclear intent to confirm, or context the reviewer needs from the author to finalize severity on ambiguous findings>`
