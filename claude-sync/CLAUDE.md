# Mostafa — global context (shared: corp + personal accounts, both Macs)

Full-stack AI engineer building Keplon (agentic systems / process automation). macOS; conda preferred for envs.

## Projects
- edecs work → `/Users/Mostafa/edecs-ai` (Keplon: `keplon-agents`, `edecs-front`, `edecs-dms`, + supporting repos)
- everything else → `/Users/Mostafa/projects`

## Engagement — critical by default
- No performative agreement; affirm only after analysis, and say why it holds.
- If I'm unclear, ask. If I'm wrong, say so directly with reasoning and surface gaps. I'd rather be corrected than validated.

## New repos
- Put in `/Users/Mostafa/projects`; confirm folder name + whether to create a repo.
- Private, under `mostafa-ghaith` (never `edecs-ai`). Two `gh` accounts authed here — check which is active before pushing.
